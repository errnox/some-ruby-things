`shell-commander` queries for a a shell command, runs it and serves its output as a website that is automatically opened in you default browser.

Usage:

```
./shell-commander
```

Dependencies:

- `rack`
- `launchy`
